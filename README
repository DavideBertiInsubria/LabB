Comandi Per L'esecuzione:

N.B. L'applicativo è stato testato solo su windows e linux.

ATTENZIONE: prima di procedere bisogna importare il database su postgresql, e per farlo basta seguire questi passaggi:

			1) Creare un database su postgre denominato 'dblabb' (per esempio con pgAdmin)
			2) Importare il database tramite il file 'dblabbstructure.sql' situato nella cartella del progetto (oppure copiare ed incollare il suo contenuto nel Query Tool del database)
			3) Eseguire al Query Tool il seguento comando:
					CREATE USER admin WITH PASSWORD 'admin';

Fatto ciò si può procedere con l'esecuzione.

Seguire i seguenti passi:

1) Estrarre il file Berti_740665 dove si desidera (es. Desktop).
   Si otterà una cartella denominata LabB

2) Aprire un terminale e recarsi nella posizione della cartella del progetto
	es.  - Cartella situata sul desktop, digitare:
	
			cd Desktop\LabB
			
3) digitare:

			mvn clean compile package install 

WINDOWS:

4) 	Per avviare il SERVER eseguire:

			java -jar target/Server-jar-with-dependencies.jar
			
	Per avviare il CLIENT CENTRIVACCINALI eseguire:
	
			java --module-path ".\lib\javafx-sdk-15.0.1\lib" --add-modules javafx.controls,javafx.fxml -jar target/ClientCentriVaccinali-jar-with-dependencies.jar
	
	Per avviare il CLIENT CITTADINI eseguire:
	
			java --module-path ".\lib\javafx-sdk-15.0.1\lib" --add-modules javafx.controls,javafx.fxml -jar target/ClientCittadini-jar-with-dependencies.jar
			

Si consiglia di aprire due CMD, uno per eseguire il server e uno per i client.



LINUX:

4) 	Per avviare il SERVER eseguire:

			java -jar target/Server-jar-with-dependencies.jar
			
	Per avviare il CLIENT CENTRIVACCINALI eseguire:
	
			java --module-path "./lib/javafx-sdk-15.0.1/lib"  --add-modules javafx.controls,javafx.fxml -jar target/ClientCentriVaccinali-jar-with-dependencies.jar
	
	Per avviare il CLIENT CITTADINI eseguire:
	
			java --module-path "./lib/javafx-sdk-15.0.1/lib"  --add-modules javafx.controls,javafx.fxml -jar target/ClientCittadini-jar-with-dependencies.jar
			

Si consiglia di aprire due terminali, uno per eseguire il server e uno per i client.
